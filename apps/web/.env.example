# Angular uses src/environments/environment.ts for configuration — not .env files.
# Edit apps/web/src/environments/environment.ts to change the API URL.
#
# Example:
#   apiUrl: 'http://localhost:4000'          (local)
#   apiUrl: 'https://your-api.azurewebsites.net'  (production in environment.prod.ts)
